{
  "buildCommand": "cd explorer && yarn install && yarn build",
  "outputDirectory": "explorer/dist",
  "installCommand": "cd explorer && yarn install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
